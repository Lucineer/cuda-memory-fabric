[package]
name = "cuda-memory-fabric"
version = "0.1.0"
edition = "2021"
description = "Shared memory fabric for multi-VM FLUX fleets — typed, permission-gated, conflict-resolved"

[dependencies]

[dev-dependencies]
